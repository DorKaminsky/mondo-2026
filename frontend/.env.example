# Backend URL for production builds. Leave empty in dev — Vite proxy handles `/api` → localhost:3001.
# In production, set this to your deployed backend, e.g. https://mondo-2026.fly.dev
VITE_API_URL=
